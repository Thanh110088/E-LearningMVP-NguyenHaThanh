PORT=5050
NODE_ENV=development
DATABASE_URL="postgresql://postgres:postgrespassword@localhost:5432/elearning_db?schema=public"

JWT_SECRET="super-secret-jwt-key-elearning-2026"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="super-secret-refresh-key-elearning-2026"
JWT_REFRESH_EXPIRES_IN="7d"

CORS_ORIGIN="http://localhost:5173"
FRONTEND_URL="http://localhost:5173"

# Google OAuth (Google Identity Services — Client ID)
GOOGLE_CLIENT_ID=""

# SMTP — để trống thì email được ghi log (dev/test outbox)
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="ETech <noreply@elearning.local>"

EMAIL_VERIFY_EXPIRES_IN="24h"
PASSWORD_RESET_EXPIRES_IN="15m"
